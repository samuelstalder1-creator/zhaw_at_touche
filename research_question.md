# Research Question

> **Does the semantic delta between a response and its neutral rewrite provide a sufficient signal for advertisement detection — and does it substitute for or complement end-to-end fine-tuning? How does access to the query and multiple neutral sources affect each approach?**

Two strategies exist for exploiting a neutral rewrite (a version of the response with advertising removed, generated by Gemini or Qwen):

- **Fine-tuned BERT**: backbone trained end-to-end on the full input
- **Delta-based LR**: frozen encoder + logistic regression on `response_emb − neutral_emb`

---

## Clean Model Comparison

### Fine-tuned models

| Input | Setup |
|---|---|
| response | missing |
| query + response | setup6 ✓, setup12 ✓ |
| query + response + 1 neutral | setup7-qwen ✓ |
| query + response + 2 neutrals | missing |

### Delta models

| Input | Setup |
|---|---|
| response − 1 neutral | setup103 ✓ |
| response − 1 neutral + query | missing |
| response − 2 neutrals | setup113 (pending) |
| response − 2 neutrals + query | missing |

---

## What We Know

- **Fine-tuned classifiers are strong without the neutral**: setup6 (RoBERTa) reaches 0.9975 F1 on query + response alone.
- **Neutral in prompt does not help fine-tuned models**: setup7-qwen (Longformer + Qwen neutral) scores 0.9964 — FN triples vs setup6-qwen (0.9985). The classifier already captures ad language from training; appending the neutral adds noise.
- **Delta direction matters, not magnitude**: cosine thresholding (setup100–102) scores 0.35–0.45 F1. Logistic regression on the full 768-dim delta vector (setup103) reaches 0.9913. Collapsing the delta to a scalar discards the directional information that makes ads detectable.
- **Scalar bottleneck explains setup110**: six pairwise cosine distances from two providers still score only 0.5653 F1. The dual-provider advantage is wasted at the scalar bottleneck.
- **Absolute positions shift precision/recall**: setup104 adds `[response_emb | neutral_emb]` alongside the delta — FP drops (23→10) but FN rises (23→35). More conservative, not strictly better.
- **Sentence-level delta has a false positive problem**: setup106 (Hungarian matching) reaches FN=37 but FP=120. Normal stylistic variation between response and neutral triggers the classifier on clean responses.
- **DeBERTa-v3 collapses to all-positive twice**: setup8 and setup105 both failed under the same conditions. Setup105 also lacked a validation config, so the cross-encoder was evaluated with the wrong input format.

---

## Open Tasks

### Missing matrix cells
- [ ] Fine-tuned: **response only** — isolate query contribution to BERT family
- [ ] Fine-tuned: **query + response + 2 neutrals** — does a second neutral help the prompted classifier?
- [ ] Delta: **response − 1 neutral + query** — does the query sharpen the delta signal?
- [ ] Delta: **response − 2 neutrals + query** — full dual-provider delta with query

### Pending implementations
- [ ] Implement `cross_encoder` backend → run setup105_1 (RoBERTa, response + neutral, joint attention)
- [ ] Implement `dual_residual_classifier` backend → run setup113
- [ ] Implement `dual_embedding_classifier` backend → run setup114

### Pending runs
- [ ] setup7 (Gemini neutral in prompt) — confirm neutral-in-prompt finding holds across providers
- [ ] setup9, setup11 — complete backbone comparison within classifier family

### Neutral quality
- [ ] Identify cases where the neutral failed to remove the ad (small delta, positive label) — these set the ceiling for all delta methods
- [ ] Compare Gemini vs Qwen neutralisation quality across ad and non-ad responses
